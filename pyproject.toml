[build-system]
requires = ["setuptools>=61.0", "cython"]
build-backend = "setuptools.build_meta"

[project]
name = "knownet"
version = "0.4.0"
description = "A graph-based tool for organizational network analysis."
readme = "README.md"
requires-python = ">=3.8"